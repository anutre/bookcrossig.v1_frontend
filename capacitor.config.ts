import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bookcrossing.io',
  appName: 'Bookcrossing',
  webDir: 'dist/book-crossing/browser',
  bundledWebRuntime: false,
};

export default config;
